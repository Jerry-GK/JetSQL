框架问题：
1. Field的正反序列化不自带buffer推后功能，封装性不好
2. 任何同类型值（包括非NULL）与NULL对比皆返回kNULL， 不方便测试，NULL和NULL相比可多返回一种结果
3.Row中并不含有魔数，row的header中的field_Num非必要?
4.table_page没有提供直接获取最后一个rowid的方法？
5. 索引迭代器机制奇怪，导致封装性不好。
6. catalog的createindex并没提供索引类型有关的参数
7. drop index只指定index名、不指定表名极不合理。
8. column有nullable成员，解释器不支持nullable定义？
9.catalog没有提供从文件上删除database的接口
10.int和float的type没有提供getdata
11.主键信息是否由catalog管理更好？包括自动在主键上建立索引
12.每张表自身都不知道其索引信息，必须贼executor里手动同步，是否合理？能否让index依赖于table？
13.解释器没怎么封装，给执行文件带来麻烦

最新版本已更新：
1.多条件查询（file scan）
2.修复了execfile的parse error提示，并且不需要一条指令占一行，每条指令也能显示时间
3.简单的异常输入处理（字符串长度定义）
4.美化界面，取消冗余输出

待处理问题:
--------emergent and difficult-----------------------
0. （Bug）updatetuple时的insertentry的必重复问题
1. index迭代器支持的<利用索引范围查找>功能, 对上层的封装。空index的scankey的bug
2. （Bug）将一张大表全部delete，有时会seg fault
3. （Bug）drop database的时候seg fault
(drop table一般不会seg fault)

--------easy and detailed------------------------
4. 中途异常退出时的db文件内容丢失问题
5. 界面美化，复杂的异常输入处理

--------no need-----------------------
6. Hash索引
7. 解释器改进：运算符优先级判定，not null声明支持，封装性
8. 事务、并发
9. join功能
